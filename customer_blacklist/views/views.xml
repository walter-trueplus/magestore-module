<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="customer_blacklist.list">
      <field name="name">customer_blacklist list</field>
      <field name="model">customer_blacklist.customer_blacklist</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="customer_blacklist.action_window">
      <field name="name">customer_blacklist window</field>
      <field name="res_model">customer_blacklist.customer_blacklist</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="customer_blacklist.action_server">
      <field name="name">customer_blacklist server</field>
      <field name="model_id" ref="model_customer_blacklist_customer_blacklist"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="customer_blacklist" id="customer_blacklist.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="customer_blacklist.menu_1" parent="customer_blacklist.menu_root"/>
    <menuitem name="Menu 2" id="customer_blacklist.menu_2" parent="customer_blacklist.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="customer_blacklist.menu_1_list" parent="customer_blacklist.menu_1"
              action="customer_blacklist.action_window"/>
    <menuitem name="Server to list" id="customer_blacklist" parent="customer_blacklist.menu_2"
              action="customer_blacklist.action_server"/>
    -->
  </data>
</odoo>