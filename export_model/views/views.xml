<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="export_model.list">
      <field name="name">export_model list</field>
      <field name="model">export_model.export_model</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="export_model.action_window">
      <field name="name">export_model window</field>
      <field name="res_model">export_model.export_model</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="export_model.action_server">
      <field name="name">export_model server</field>
      <field name="model_id" ref="model_export_model_export_model"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="export_model" id="export_model.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="export_model.menu_1" parent="export_model.menu_root"/>
    <menuitem name="Menu 2" id="export_model.menu_2" parent="export_model.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="export_model.menu_1_list" parent="export_model.menu_1"
              action="export_model.action_window"/>
    <menuitem name="Server to list" id="export_model" parent="export_model.menu_2"
              action="export_model.action_server"/>
    -->
  </data>
</odoo>